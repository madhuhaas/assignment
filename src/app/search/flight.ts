/* Defines the search details entity */
export interface Flight {
  origin: string;
  destination: string;
  date: string;
  departureTime: string;
  arrivalTime: string;
  flightNo: String;
  price: number;
  name: string;
  duration: string;
  totalPrice: number;
}